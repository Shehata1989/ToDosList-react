import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    fontFamily: '"Alexandra", sans-serif',
    fontWeight: 700,
  },
  palette: {
    primary: {
      main: "#163f3d",
    },
    secondary: {
      main: "#f44336",
    },
  },
});